export const metadata = {
    title: 'Naruto DB || Teams',
    description: 'A Naruto Database for Fun',
}

export default function ({ children }) {
    return (
        <>
            {children}
        </>
    )
}
